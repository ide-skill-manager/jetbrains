.gradle/
build/
*.class
.idea/
*.iml
out/
.DS_Store
.intellijPlatform/

# Kotlin compiler artefacts
.kotlin/

# BasePlatformTestCase scratch (created when tests run on filesystems that allow `:`)
temp:/

# Components Agentry installs into THIS project when developers exercise the plugin
# via `./gradlew runIde` and point it at a registry. These are dev artefacts, not source.
.claude/
.github/agents/
.github/hooks/
.github/mcp/
.github/prompts/
.github/instructions/
